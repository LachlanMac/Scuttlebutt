using UnityEngine;

namespace Starbelter.Core
{
    /// <summary>
    /// Attribute to mark a string field as a Position ID.
    /// In the inspector, this will show a dropdown of all available positions.
    /// </summary>
    public class PositionIdAttribute : PropertyAttribute
    {
    }
}
